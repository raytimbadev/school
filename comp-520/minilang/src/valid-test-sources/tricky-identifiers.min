# Uses identifiers that contain keywords as substrings

var reader : string;
var printer : string;
var integer : int;

print "What is your name?";
read reader;
printer = reader;
print "The countdown is starting !";

integer = 10;

while integer do
    print integer;
    integer = integer - 1;
done

print "Happy new year, " + printer;
